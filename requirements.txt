# Dipendenze per sviluppo/test in locale (scripts/, tests/), SENZA Docker.
# Le dipendenze complete di backend/frontend sono nei rispettivi
# requirements.txt, usati dai Dockerfile.
crawl4ai==0.9.0
pydantic>=2.7,<3
beautifulsoup4==4.12.3
mistune==3.0.2
rouge-score==0.1.2
